
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RMS || Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
      <link rel="stylesheet" media="screen, print" href="<%= Routes.static_path(@conn, "/css/login/vendors.bundle.css") %>" />
        <link rel="stylesheet" media="screen, print" href="<%= Routes.static_path(@conn, "/css/login/default.css") %>" />
        <link rel="stylesheet" media="screen, print" href="<%= Routes.static_path(@conn, "/css/login/app.bundle.css") %>" />
	
    <style>
        body {
            background-image: url('images/4.jpeg');
            background-size: cover;
            background-position: center;
            height: 100vh;
        }
        .login-container {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            padding: 30px;
            max-width: 400px;
            margin: auto;
        }
        .btn-primary {
            background-color: #3A6E35;
            border-color: #3A6E35;
        }
        .btn-primary:hover {
            background-color: #2C5228;
            border-color: #2C5228;
        }
        .logo {
            max-width: 150px;
            margin-bottom: 20px;
        }
        .zambia-flag {
            width: 30px;
            height: 20px;
            display: inline-block;
            margin-right: 10px;
            background: linear-gradient(to bottom, 
                #008751 33.33%, 
                #ef3340 33.33%, 66.66%, 
                #000000 66.66%, 100%);
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <div class="login-container">


            <div class="row">
                <div class="">
                    <div class="form-group">
                        <%= if get_flash(@conn, :info) do %>
                        <div class="alert alert-success alert-styled-left">
                            <button type="button" class="close" data-dismiss="alert">&times; <span class="sr-only">Close</span></button>
                            <%= get_flash(@conn, :info) %>
                        </div>
                        <% end %>
                        <%= if get_flash(@conn, :error) do %>
                        <div class="alert alert-danger alert-styled-left">
                            <button type="button" class="close" data-dismiss="alert">&times; <span class="sr-only">Close</span></button>
                            Opps! <%= get_flash(@conn, :error) %>
                        </div>
                        <% end %>
                    </div>
                </div>
            </div>


            <div class="text-center mb-4">
                <h2 class="mb-3">Railway Management System</h2>
                <div class="text-center">
                     <div class="text-center mb-4">
                    <span class="fw-bold">Zambia Railways Limited</span>
                    </div>
                </div>
            </div>
            <form  id="js-login" action="<%= Routes.session_path(@conn, :create)%>" method="POST">
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control" placeholder="Your Email" name="email" required>
                    <input type="hidden" name="_csrf_token" value="<%= Plug.CSRFProtection.get_csrf_token() %>">
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" id="password" name="password" class="form-control" placeholder="Your Password" required>
                </div>
                <div class="mb-3 form-check">
                    <input type="checkbox" class="form-check-input" id="rememberMe">
                    <label class="form-check-label" for="rememberMe">Remember me</label>
                </div>
                <button type="submit" id="login" class="btn btn-primary w-100">Login</button>
            </form>
            <div class="text-center mt-3">
                <a href="/forgort/password" class="text-decoration-none">Forgot password?</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>