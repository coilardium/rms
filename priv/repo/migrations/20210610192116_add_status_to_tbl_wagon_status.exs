defmodule Rms.Repo.Migrations.AddStatusToTblWagonStatus do
  use Ecto.Migration

  def up do
    # alter table(:tbl_wagon_status) do
    # add :status, :string
    # end
  end

  def down do
    # alter table(:tbl_wagon_status) do
    # remove :status
    # end
  end
end
